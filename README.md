# vcokltfre/StarsongBot

### A Discord bot for the Sanctuary server

Config structure in `config/config.py`:
```py
name = 'Starsong'
log_level = 'info'
log_type = 'text'
token = 'token'
hook = 'webhook_url'
dev_ids = [discord_id]
```
